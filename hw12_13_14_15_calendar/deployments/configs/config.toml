# Example config

[logger]
level = "info"

[server]
host = "0.0.0.0"
port = 3000
gateway_port = 3001
shutdown_timeout = 3000 # ms

[database]
storage_type = "sql" # available storages: `inmemory` | `sql`
host = "postgres"
port = 5432
username = "postgres"
password = "postgres"
db_name = "calendar"
ssl_mode = "disable"
db_timeout = 3000 # ms
max_connections = 5
